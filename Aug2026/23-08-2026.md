# Check Divisibility by Digit Sum and Product

> **LeetCode Problem #3622** | [View on LeetCode](https://leetcode.com/problems/check-divisibility-by-digit-sum-and-product/)  
> **Difficulty:** Easy  
> **Topics:** Math, Digit Manipulation

---

## 📝 Problem Statement

Given a positive integer `n`, determine whether `n` is divisible by the **sum** of the following two values:

1. The **digit sum** of `n` (the sum of its digits).
2. The **digit product** of `n` (the product of its digits).

Return `true` if `n` is divisible by this total sum; otherwise, return `false`.

---

## 🔍 Examples

### Example 1
```

Input: n = 99
Output: true

```
**Explanation:**  
- Digit Sum = 9 + 9 = 18  
- Digit Product = 9 * 9 = 81  
- Total Divisor = 18 + 81 = 99  
- Since 99 % 99 == 0, return `true`.

### Example 2
```

Input: n = 23
Output: false

```
**Explanation:**  
- Digit Sum = 2 + 3 = 5  
- Digit Product = 2 * 3 = 6  
- Total Divisor = 5 + 6 = 11  
- Since 23 % 11 != 0, return `false`.

---

## ⚙️ Constraints

- `1 <= n <= 10^6`

---

## 🧠 Intuition

The problem is straightforward and purely mathematical. We just need to:

1. Extract each digit of `n`.
2. Compute the running sum and running product simultaneously.
3. Add them together to get the divisor.
4. Check if `n` is perfectly divisible by this divisor.

> 💡 **Edge Case:** For single-digit numbers, the digit product equals the digit itself, and the divisor is `2 * digit`. The solution still works correctly.

---

## 📐 Algorithm

```

function checkDivisibility(n):
sum = 0
product = 1
temp = n

```

---

## 💻 Code (Java)

```java
class Solution {
    public boolean checkDivisibility(int n) {
        int temp = n;
        int digitSum = 0;
        int digitProduct = 1;

        while (temp > 0) {
            int digit = temp % 10;
            digitSum += digit;
            digitProduct *= digit;
            temp /= 10;
        }

        int divisor = digitSum + digitProduct;
        return n % divisor == 0;
    }
}
```

---

⏳ Complexity Analysis

· Time Complexity: O(log₁₀ n) — We iterate over each digit of the number.
· Space Complexity: O(1) — We use only a few integer variables.

---

🧪 Test Cases

n Digit Sum Digit Product Divisor Divisible? Output
99 18 81 99 ✅ Yes true
23 5 6 11 ❌ No false
12 3 2 5 ❌ No false
36 9 18 27 ❌ No false
10 1 0 1 ✅ Yes true
7 7 7 14 ❌ No false

---

🚀 How to Run

1. Copy the code into a file named Solution.java.
2. Compile using:
   ```bash
   javac Solution.java
   ```
3. Run the main method (if added) or test via LeetCode's built-in runner.

---

📌 Notes

· The product of digits for numbers containing 0 is always 0. The divisor in such cases is simply the digit sum.
· This solution passes all constraints comfortably within the given limits.

---

⭐ If you found this useful, don't forget to star the repository! ⭐

```

---

**"Thalaiva" special note:** This README is fully detailed and ready to push to your GitHub. If you want me to add a Tamil section at the bottom or customize it further (like adding your personal social links), just let me know! 😎